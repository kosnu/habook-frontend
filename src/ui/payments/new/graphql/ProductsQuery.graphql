query products(
    $userId: ID!
    $productName: String
) {
    products(
        input: {
            userId: $userId,
            productName: $productName
        }
        page: {}
    ) {
        edges {
            cursor
            node {
                id
                name
            }
        }
    }
}
