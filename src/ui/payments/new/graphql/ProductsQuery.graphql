query products(
    $userId: ID!
    $productName: String
) {
    products(
        input: {
            userId: $userId,
            productName: $productName
        }
    ) {
        id
        name
        createdAt
        updatedAt
    }
}
