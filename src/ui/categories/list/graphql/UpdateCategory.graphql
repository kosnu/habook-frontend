mutation updateCategory(
    $id: ID!
    $userId: ID!
    $name: String!
) {
    updateCategory(
        input: {
            id: $id,
            userId: $userId,
            name: $name
        }
    ) {
        ...Categories_Category
    }
}
