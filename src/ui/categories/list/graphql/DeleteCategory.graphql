mutation deleteCategory(
    $id: ID!
    $userId: ID!
) {
    deleteCategory(
        input: {
            id: $id,
            userId: $userId
        }
    ) {
        ...Categories_Category
    }
}
