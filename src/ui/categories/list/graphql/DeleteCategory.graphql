fragment Category on Category {
    __typename
    id
    name
    enable
}

mutation deleteCategory(
    $id: ID!
    $userId: ID!
) {
    deleteCategory(input: {id: $id, userId: $userId}) {
        ...Category
    }
}
